{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "mkdir -p /app/writable/{cache,debugbar,logs,session} && chmod -R 775 /app/writable && chown -R www-data:www-data /app/writable && php-fpm -y /app/php-fpm.conf -D && nginx -c /app/nginx.conf -g 'daemon off;'",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}